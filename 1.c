#include <stdio.h>
#include <math.h>

int main() {

    int a, b, c;

    int o = pow(a, 3) + pow(a,3) + pow(a,3);

    int f = ((((3+4) * 0))) * (int)(pow(a,pow(b,c))) * 7000*a*a | c;

    int l = (a+b) + 7 +(a+b);

    a = log(10) + c + b + c + 4 + b + c + 2 + 3;

    b = 1 + 1 + b*a*b*a*b*b*b * 1 + a + 2 + a + 4 * b*f * 16 & -1;

    c = a*b*a*b*5 * 0;

    int x = 4*(int)(pow(c,a))*1 | -1;

    int p = 3*a*400 + 0 + 1 + 0 + 0;

    if(a + 0 < c)
        printf("\n");


    return 0;
}
